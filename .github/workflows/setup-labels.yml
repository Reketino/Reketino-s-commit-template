name: Setup Labels

on:
    push:
      branches:
        - main
    workflow_dispatch:

jobs: 
  setup-labels:
    runs-on: ubuntu-latest

    steps:
      - name: Create or update labels
        uses: actions/github-script@v7